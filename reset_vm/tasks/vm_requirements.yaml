---

# TODO add handling for debian systems
- name: Install libvirt dependencies
  ansible.builtin.package:
    name: libvirt-devel
    state: present
  become: true

- name: Install python dependencies
  ansible.builtin.package:
    name: python-devel
    state: present
  become: true

- name: Install pip libvirt package
  ansible.builtin.pip:
    name: libvirt-python
    state: present
  become_user: mainws

...
