---
# vars file for gnome_config

prerequired_packages:
  - gnome-themes-extra  # includes Adwaita-dark
  # - pip

gnome_config_apps:
  - gnome-tweaks
  - gnome-extensions-app

pip_packages:
  - psutil

gnome_basic_config_file: "gnome_basics_config.yml"

...
