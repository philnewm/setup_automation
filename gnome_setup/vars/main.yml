---
# vars file for gnome_config

dependencies:
  - python3-psutil
  - git
  # TODO move to extensions dictionary
  - make  # required for dash-to-panel extension
  - gettext  # required for dash-to-panel extension
  - zip  # required for tiling-assistent extension

...
